
  <%= form_with(model: [project, ticket, ticket.issues.build]) do |f|%>
    <h3 class="pb-2 font-bold"> Replies </h3>
    <%= f.rich_text_area :body, class: "w-[100%] min-h-[400px] border border-gray-300 rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:placeholder-gray-400  dark:focus:ring-blue-500 dark:focus:border-blue-500" %>
    <%= f.submit "Submit",  class: 'mt-4 w-2/5 h-12 border border-b-4 border-r-2 p-3 font-semibold rounded'  %>
  <% end %>
