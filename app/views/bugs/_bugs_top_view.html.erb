<div class="grid grid-cols-2 ">
  <div class="grid col-span-1">
        <div class="flex gap-1">
          <%= button_to product_board_task_path(@product.id, @board.id, @task.id), method: :get,  class: 'mb-4 h-12 bg-[#3F8CFF] hover:bg-[#3A81EB] p-3 rounded font-bold rounded-lg text-slate-100'  do %>
            &#8656; Back
          <% end %>
        </div>
  </div>
  <!-- Search on the right -->
</div>

