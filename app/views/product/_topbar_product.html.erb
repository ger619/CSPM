<div class="grid grid-cols-2 ">
  <div class="grid col-span-1">
    <% if can? :create, @product %>
      <% if current_user.has_role? :admin or current_user.has_role?('project manager') %>
        <div class="flex gap-1">
          <%= button_to root_path, method: :get,  class: 'mb-4 h-12 bg-[#3F8CFF] hover:bg-[#3A81EB] p-3 rounded font-bold rounded-lg text-slate-100' do %>
            &#8656; Back
          <% end %>
        <%= button_to 'Add Project +', new_product_path, method: :get,  class: 'mb-4 h-12 bg-[#3F8CFF] hover:bg-[#3A81EB] p-3 rounded font-bold rounded-lg text-slate-100', id: 'button' %>
        </div>
      <% end %>
    <% end %>
  </div>
  <!-- Search on the right -->
  <div class="grid col-span-1 justify-end">
    <div class="flex flex-col md:flex-row items-start md:items-center w-full">
      <%= form_with url: product_index_path, method: :get, class: 'flex gap-2' do |f| %>
        <%= f.text_field :query, placeholder: "Search ", class: "border-b-4 h-12 border border-black dark:border-slate-100 border-b-4 border-r-2 rounded" %>
        <%= f.submit "Search",  class: 'mb-4 h-12 bg-[#3F8CFF] hover:bg-[#3A81EB] p-3 rounded font-bold rounded-lg text-slate-100'  %>
      <% end %>
      <%= form_with url: product_index_path, method: :get, class: 'flex' do |f| %>
        <%= f.submit "Clear", class: 'ml-2 mb-4 h-12 bg-[#3F8CFF] hover:bg-[#3A81EB] p-3 rounded font-bold rounded-lg text-slate-100'  %>
      <% end %>
    </div>
  </div>
</div>