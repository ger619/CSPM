<div class="grid grid-cols-2 ">

  <div class="grid col-span-1">
    <% if can? :create, @project %>
      <% if current_user.has_role? :admin or current_user.has_role?('project manager') %>
        <%= link_to 'Add New Project +', new_project_path, class: 'mb-4 w-2/5 h-12 border border-black dark:border-white border-b-4 border-r-2 p-3 rounded font-semibold rounded'  %>
      <% end %>
    <% end %>
  </div>
  <!-- Search on the right -->
  <div class="grid col-span-1 justify-end">


  <div class="flex flex-col md:flex-row items-start md:items-center w-full">
    <%= form_with url: project_index_path, method: :get, class: 'flex' do |f| %>
      <%= f.text_field :query, placeholder: "Search ", class: "border-b-4 h-12" %>
      <%= f.submit "Search", class: "mb-4 ml-4 h-12 border border-black dark:border-gray-800 border-b-4 border-r-2 p-2 rounded font-semibold rounded"  %>
    <% end %>

    <%= form_with url: project_index_path, method: :get, class: 'flex' do |f| %>
      <%= f.submit "Clear", class: "ml-4 mb-4 h-12 border border-black dark:border-gray-800 border border-black  border-b-4 border-r-2 p-2 rounded font-semibold rounded"  %>
    <% end %>

  </div>
  </div>

</div>