<div class="relative overflow-x-auto shadow-md sm:rounded-lg w-[100%] ">
  <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
    <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
    <tr>
      <th scope="col" class="px-2 py-2">
        Name of the Software Product
      <th scope="col" class="px-2 py-2">
        Description
      </th>
      <th scope="col" class="px-2 py-2">
        Project Manager
      </th>
      <th scope="col" class="px-2 py-2">
        EDIT
      </th>
    </tr>
    </thead>
    <tbody>
    <% @groupware.each do |group| %>
      <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-600 h-[12px]">

        <th scope="row" class="px-2 py-2 font-medium text-gray-900 whitespace-nowrap dark:text-white">
          <%= group.name   %>
        </th>
        <th scope="row" class="px-2 py-2 font-medium text-gray-900 whitespace-nowrap dark:text-white capitalize">
          <%= group.description %>
        </th>
        <th scope="row" class="px-2 py-2 font-medium text-gray-900 whitespace-nowrap dark:text-white capitalize">
          <%= group.user.present? ? group.user.name : 'No User Assigned' %>
        </th>
        <th scope="row" class="px-2 py-2 font-medium text-gray-900 whitespace-nowrap dark:text-white capitalize">
          <%= link_to 'Edit', edit_software_groupware_path(group.software, group), class: 'text-blue-500' %>
        </th>
        <!--
        <th scope="row" class="px-2 py-2 font-medium text-gray-900 whitespace-nowrap dark:text-white uppercase">
          <%#= link_to 'Delete', [group.software, group], data: { turbo_confirm: 'Are you sure?' }, class: 'text-blue-500' %>
        </th>
        -->


      </tr>

    <% end %>
    </tbody>
  </table>
</div>

