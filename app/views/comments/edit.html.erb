<%= turbo_frame_tag 'modal' do %>
  <div class="modal">
    <h2>Edit Comment</h2>
    <%= render 'comments/edit_form', project: @project, ticket: @ticket %>
    <%= link_to 'Cancel', "#", data: {
      controller: 'modals',
      action: 'modals#close'
    }, class: "cancel-button"%>
  </div>
<% end %>