<%= turbo_frame_tag 'modal' do %>
  <div class="modal h-[90%]">
    <%= render 'comments/form', project: @project, ticket: @ticket %>
    <!-- Cancel Button -->
    <%= link_to 'Cancel', cancel_redirect_path, data: { turbo: false }, class: "cancel-button px-4 py-2 h-12 border border-b-4 border-r-2 border-black dark:border-slate-100 p-3 font-semibold rounded" %>
  </div>
<% end %>
